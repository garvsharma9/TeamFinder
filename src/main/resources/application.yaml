spring:
  application:
    name: TeamFinder
  mongodb:
    uri: mongodb+srv://garvroc9_db_user:cEDSfSFocc1v7BeY@cluster0.xweqsf8.mongodb.net/teamfinder

server:
  port: ${PORT:8080}

app:
  base-url: ${APP_BASE_URL:https://garvsharma9-teamfinder-api.hf.space}

# --- NEW BREVO API CONFIGURATION ---
brevo:
  api-key: ${BREVO_API_KEY}
  sender-email: ${BREVO_SENDER_EMAIL}
# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}
google:
  client:
    id: ${GOOGLE_CLIENT_ID}

github:
  client:
    id: ${GITHUB_CLIENT_ID}
    secret: ${GITHUB_CLIENT_SECRET}